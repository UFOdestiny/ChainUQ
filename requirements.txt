# Core deep learning
torch>=2.9.1
transformers>=4.57.3
accelerate>=1.12.0
datasets>=4.3.0

# Inference engine
vllm>=0.4.0

# Attention acceleration (optional but recommended for long-context generation)
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# Scientific computing
numpy>=1.24.1
scipy>=1.12.0
scikit-learn>=1.4.0

# Visualization
matplotlib>=3.8.4

# Data handling
pyarrow>=22.0.0
tqdm>=4.67.1
pyyaml>=6.0.1

# Optional: experiment tracking (enable with REPORT_TO=wandb)
# wandb>=0.16.0
